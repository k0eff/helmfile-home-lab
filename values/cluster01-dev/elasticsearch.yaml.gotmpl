replicas: 1
secret:
    enabled: true
    password: {{ requiredEnv "ES_CREDS_PASSWORD" }}
resources:
  requests:
    cpu: "100m"
    memory: "2Gi"
volumeClaimTemplate:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi