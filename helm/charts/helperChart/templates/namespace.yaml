{{- if .Values.namespace }}
{{- if .Values.namespace.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels: 
{{- range $key, $value := .Values.namespace.labels }}
    {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
{{- end }}